# Tables

One file per table worth a standalone deep-dive — full column list, meaning, gotchas. Keep `../box-data-model.md` as the short map of which tables matter and how they relate; put the detail on any one of them here once it's substantial enough to crowd that overview.

Name files after the table: `entity.md`, `coa_mapping.md`.
