# BOX — APIs / Integration Points

## Endpoints or interfaces used during onboarding

| Name | Type (REST/batch/DB write/Camunda task) | Auth | Idempotent? | Notes |
|---|---|---|---|---|

## Camunda process(es) involved

Process definition key(s), what triggers them, what signals/messages an agent needs to send or wait on.

## Environments

Dev / UAT / Prod endpoints and how credentials are supplied (never put actual secrets here — reference the vault/secret-store path only).
