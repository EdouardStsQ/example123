# Example: <what this demonstrates>

## Starting state

What existed before this sequence ran (static data, prior BOX state).

## Sequence

Step-by-step, as it actually ran — include the actual skill/agent calls and BOX responses (with real values replaced by placeholders).

## Outcome

Final state, and how it was confirmed correct.

## What this example is useful for

E.g. "reference case for the happy-path eval", "shows what a static-data failure looks like mid-sequence".
